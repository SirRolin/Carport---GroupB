package app.entities;

public class PillerDTO extends MaterialDTO {
    public PillerDTO(String info, int amount, String unitType, String costumInfo) {
        super(info, amount, unitType, costumInfo);
    }
}
